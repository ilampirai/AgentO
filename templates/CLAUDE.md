# AgentO Project

This project uses AgentO. Use `/agento <prompt>` for tasks.

When `/agento` is used, all file operations must use:
- `agento_write` (not Write)
- `agento_read` (not Read)
- `agento_bash` (not Bash)
- `agento_search` (not Grep/Glob)

